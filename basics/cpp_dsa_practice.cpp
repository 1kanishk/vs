#include <iostream>
#include <vector>
#include <string>
#include <algorithm>
using namespace std;
int main()
{
    int arr[] = {4,1,5,2,3};
    int curr = 0;
    int n = sizeof(arr) / sizeof(arr[0]);
    for (int i = 1; i < n; i++)
    {
        curr = arr[i];
        
    }
    
    return 0;
}